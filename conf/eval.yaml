
dataset_path: /home/yasas/UNSW/Adv_Temporal/SceneFlow/argoverse2/processed_test/sensor
checkpoint: /home/yasas/UNSW/Adv_Temporal/SceneFlow/SceneFLow-Attack/ckpts/deflow_official.ckpt  #/home/yasas/UNSW/Adv_Temporal/SceneFlow/SceneFLow-Attack/ckpts/fastflow3d_best.ckpt
av2_mode: val # [val, test]
save_res: False # [True, False]

# no need to change
slurm_id: 00000
output: ${model.name}-${slurm_id}
gpus: 1
seed: 42069
eval_only: True
loss_fn: ff3dLoss
wandb_mode: offline # [offline, disabled, online]

defaults:
  - hydra: default
  - model: deflow